<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'user/users/info', user: current_user %>
    </div>
    <div class='col-md-8 offset-md-1'>
      <h2>レビュー一覧：<%= @reviews.count %>件</h2>
      <%= render 'index', reivews: @reviews%>
      <%= paginate(@reviews) %>
    </div>
  </div>
</div>