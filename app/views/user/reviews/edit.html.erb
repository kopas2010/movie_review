<h1>レビュー編集画面</h1>

<%= form_with model: @review, url:edit_movie_review_path(@review.movie_id, @review.id), method: :patch do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>レビュー</h4>
  <%= f.text_area :body %>

  <%= f.submit '保存' %>

<% end %>