<h1>レビュー編集画面</h1>

<%= form_with model: @review, url:admin_review_path(@review.id), method: :patch do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>レビュー</h4>
  <%= f.text_area :body %>

  <%= f.submit '保存' %>

<% end %>